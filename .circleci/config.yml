version: 2.1
orbs:
  cypress: cypress-io/cypress@1 
workflows:
  build:
    jobs:
          pre-steps: 
          - run: yarn build 
      - cypress/run:
          yarn: true
              
          start: yarn start
          wait-on: 'http://localhost:3000'
    
